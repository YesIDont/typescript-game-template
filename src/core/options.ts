export type TOptions = {
  isPaused: boolean;
  debugDraw: boolean;
  hideSystemCursor: boolean;
};

export const options: TOptions = {
  isPaused: false,
  debugDraw: false,
  hideSystemCursor: false,
};
